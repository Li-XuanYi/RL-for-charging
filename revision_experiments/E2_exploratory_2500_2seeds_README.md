# 实验 2 探索版：2500 步 × 2 个种子

在原论文目录双击 `run_experiment_2_exploratory_2500_2seeds.cmd`。配置文件已放在同目录的 `revision_experiments/configs/experiment_2_exploratory_2500_2seeds.json`，运行依赖现有实验代码和已通过复核的实验 1 交接文件。

每次 2500 步，种子 7、17，决策周期 90/30/10 秒，两类训练（无保护、带保护）。总共 12 次训练、30000 次整包训练交互。验证和测试另计，仍保留原案例数、约束、充电时限和终端保持要求。

检查点保留约每 1000 步一次，另在最后 2500 步验证并保存；中间检查发生在回合边界。epsilon 按回合起点更新，在前 2000 步从 0.5 衰减到 0.05，其后保持。训练不足可能影响比较，本版仅用于初步探索，不替代正式论文证据。

结果独立保存：

```text
C:\Users\zhengzihao\Desktop\RL-for-charging-main\revision_results\experiment_2_exploratory_2500_2seeds\时间戳\
```

新版本拥有独立的完成/开始指针，不覆盖 4000 步版或正式版。实验 3—6 的正式入口不会自动采用本探索结果。

如果旧任务仍在运行，先在旧窗口按 Ctrl+C 并等待退出，再启动本入口；它从头训练，不续接旧任务。已有结果保留。本次没有启动或中断任何实验，也没有运行 Python、CMD 或测试，只做文件和配置静态校验。

备份包仅包含新增入口、配置与说明，不包含已安装的完整实验框架。若重新解压，请将内容合并到原论文目录，不要在下载目录直接运行孤立的 CMD。
