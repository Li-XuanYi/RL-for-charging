# 实验 1 精度复核与实验 2 启动修复

原结果：`revision_results/experiment_1/20260920_092446_348273`。

该次运行完成了 60 组参数搜索，但 `status.json` 为 `failed`：模型误差筛查通过，数值精度检查未通过。网格加密带来的最大电压差分别约为 6.64、6.59、6.76 mV，原定标准为 2 mV。因此没有生成 `handoff.json` 和 `LATEST_COMPLETED.txt`。实验 2 的“未找到完成结果”是后续提示，根因在实验 1 的网格精度。

本补丁没有创建虚假的完成标记，也没有改动原结果或放宽 2 mV 标准。基础网格由电极方向 30 点/颗粒径向 10 点改为 120/40，比较网格由 60/20 改为 240/80。实验 2—6 从同一 model.py 获取基础网格，避免复核和后续控制使用不同离散精度；后续物理求解可能比原网格更慢。

新增 `run_experiment_1_recheck.cmd`。它按锁定的原运行文件哈希复用 60 组已有优化结果与原来的选种子规则，重新计算细网格下的电压/温度曲线、误差、覆盖率、前缀预测、模型形式比较及数值检查；不重新进行全部 PSO 搜索。新报告明确标注“参数是在原网格上拟合，现在冻结复核”，不声称已在细网格重新寻优。原始数据、元数据、物理参数和拟合设置若被修改，将拒绝复用。

请在原论文目录操作：

1. 双击 `run_experiment_1_recheck.cmd`。
2. 只有显示 `E1 recheck passed`，新结果也生成 `handoff.json`，再双击原有 `run_experiment_2.cmd`。
3. 如果复核仍失败，保留新日志和 `summary.json`，继续定位误差或重新辨识；不能通过放宽阈值或修改完成标记让结果强行通过。

复核结果仍写入 `revision_results/experiment_1/新时间戳`。旧结果保留。正式 E1 入口仍可重新做完整的细网格参数搜索，但无需先做这一步才能尝试冻结参数复核。

已改进实验 2 的报错：会说明“拟合完成但精度未通过”，并提示使用复核入口。

本次只阅读已有结果、编写补丁及进行静态文件校验，没有运行 Python、CMD、复核或实验，不能预先保证新网格已经通过检查。原代码备份放在项目目录的 `revision_code_backups/20260920_precision_repair`。
