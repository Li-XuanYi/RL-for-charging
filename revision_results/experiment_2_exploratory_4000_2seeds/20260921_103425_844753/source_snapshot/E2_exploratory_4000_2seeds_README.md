# 实验 2 探索版：4000 步、2 个种子

入口放在原论文文件夹：`run_experiment_2_exploratory_4000_2seeds.cmd`。

- 每组训练：4000 次整包交互。
- 随机种子：7、17。
- 决策周期：90、30、10 秒。
- 两类训练：无保护、带保护。共 12 次训练，总计 48000 步。
- 每约 1000 步进行验证和保存检查点，最后 4000 步也执行；检查发生在回合边界，实际中间步数可能略超阈值。
- epsilon 保持从 0.5 衰减至 0.05，沿用预算比例 0.8，即前 3200 步完成衰减，剩余阶段维持 0.05；按回合起点更新。
- 验证集、测试集、5400 秒任务时限、270 秒终端保持、约束和失败口径均沿用正式版。验证和测试耗时不计入 48000 训练步。

配置：`revision_experiments/configs/experiment_2_exploratory_4000_2seeds.json`。

结果固定保存到：

```text
C:\Users\zhengzihao\Desktop\RL-for-charging-main\revision_results\experiment_2_exploratory_4000_2seeds\时间戳\
```

新目录有自己的 `LATEST_STARTED.txt`、`LATEST_COMPLETED.txt`，不会覆盖正式 `experiment_2` 的结果或指针。每次启动仍建立新时间戳文件夹。仍读取已通过复核的实验 1 正式交接文件，无需为缩短训练而重新做实验 1。

正式 `run_experiment_2.cmd` 和正式配置不变。新增入口从头开始探索版训练，不接着正在运行的 60000 步任务训练，也不会自动中断旧进程。如果要改跑本版，请先在旧窗口按 Ctrl+C 停止，并等待其退出，再启动新 CMD；旧运行的部分文件会保留，不具备完整断点续跑保证。

该版本用于检查流程和观察初步安全差异，不能证明收敛或稳定的算法优势。结果会标注 exploratory/formal_evidence=false。实验 3—6 的正式入口继续读取正式实验 2 目录，不会把本版结果自动当成正式上游；若后续也改探索版，需要另行一致配置。

仅完成代码编写、配置/路径及文件静态校验，没有运行 Python、CMD、训练、仿真或测试。安装时只扩展公共入口对独立结果标签的支持；不改变原实验的算法逻辑。
