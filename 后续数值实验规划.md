# 电池充电与均衡论文后续数值实验规划

依据：拒稿信、补充 Word 文档及 2026 年 9 月 19 日完成的数值主线还原结果。本文为后续实验建议，列出的实验规模、参数范围和验收门槛均为拟议方案，不是已获得的结果。继续沿用暂不开展实物实验的范围。

建议将论文的研究问题明确为：**在可独立调节各电芯充电电流的条件下，多智能体协同能否在满足电压、温度和供电约束的同时，改善异质电芯的持续均衡与充电效率，并在模型误差和未见工况下保持效果。**

执行顺序为：明确模型与公平比较条件 → 修复安全与持续均衡 → 建立强基线 → 验证组件贡献 → 测试未见工况 → 扩展规模并重写论证。优先完成前四项，再决定是否扩大算法和工况数量。

## 一 对两份材料的判断

拒稿信要求增加实质性新结果。三位审稿人的共同关注点是比较不足、QMIX 组件必要性不明、奖励及探索设置缺乏依据、安全与泛化证据不足。仅延长训练和补充几张曲线，不能充分解决这些问题。

补充 Word 文档主要整理了方法概念和审稿意见，尚未给出实验变量、分组、重复次数及判断标准。应修正以下处理方式：

- 文档把奖励敏感性标为“可以就进行文字补充”。不建议这样处理：R2 第 5 条和 R3 的奖励问题都明确要求实验，R3 还点名要求单位权重。
- 文档图片质疑式（6）的 SOC 不等号。原文定义是充电终止时达到最低目标 SOC，因此 `SOC_i ≥ SOC_target` 可以成立；另设 `SOC_i ≤ SOC_max` 表示过充边界。先澄清定义，不能直接将终止条件改成 `≤`。
- “避免放电”“不需要均衡拓扑”需要限定电流调节条件。非负动作本身就排除了放电，不能把这一结果全部归因于 RL。各电芯独立调流仍需相应充电通道或功率调节条件。
- QMIX 的价值混合网络、超网络和权重共享是不同设计。共享权重不意味着动作相同，也不意味着部署时必须运行混合网络。需要分别解释并消融。[QMIX 原始论文](https://proceedings.mlr.press/v80/rashid18a.html)

实物在线控制和微控制器实验本轮保留为未解决项。在回复中说明本轮补充数值证据，并收缩工程适用性表述，不能写成已通过仿真完全回应了硬件要求。

## 二 当前结果决定了起点

| 项目 | 当前结果 | 对后续工作的影响 |
|---|---|---|
| 数据与模型 | 三节电芯已有放电电压和温度序列，可重新辨识 | 足以搭建数值实验，不足以证明跨工况模型准确性 |
| 电压拟合 | RMSE 约 0.0201 / 0.0231 / 0.0221 V | 达到原论文误差量级，但属于同曲线拟合 |
| 温度拟合 | RMSE 约 0.404 / 0.342 / 0.413 °C | 末段升温低估，不能直接据此声称温度安全或寿命改善 |
| 训练 | A、B 各 200 回合，单一种子 | 能验证流程，不能评价跨种子稳定性 |
| 最终 SOC | 两组均满足各节 ≥0.9、标准差 ≤0.02 | 已具备充电与均衡的基本能力 |
| 电压约束 | A 最大 4.283 V，B 最大 4.384 V | 均超过 4.2 V，安全是首要问题 |
| 持续均衡 | A 再次失衡；B 首次均衡恰好出现在终止时刻 | B 的“之后保持均衡”为零持续时长，不能作为长期保持的证据 |
| 结果一致性 | 权重重新加载重演一致；高精度模型交叉核验通过 | 保存结果可信，但不等于策略收敛或论文结论成立 |

当前重建还有必须披露的假设：按原代码采用 5 A，而论文 4 Ah 电芯的 1C 口径对应 4 A；采样间隔按原脚本解释为 1 s；采用 SPMe 和 Chen2020 基础字典；数值失败额外扣 2000 分。它们需要逐项确认、保留为敏感性条件，或在新版本中有依据地修改。

## 三 实验清单与先后顺序

| 编号 | 优先级 | 实验问题 | 主要回应 |
|---|---|---|---|
| E0 | 必须先做 | 模型和实验条件是否可信、各算法是否得到相同控制权限 | R1-3；R3 数据来源、在线离线、动作空间 |
| E1 | 必须先做 | 能否在约束内完成充电并持续均衡 | R3 硬约束；R2-6 局限性 |
| E2 | 必须做 | 相对合理规则和现代 RL 方法是否有实际优势 | R1-2；R2-2；R3 DQL 质疑 |
| E3 | 必须做 | 协同、记忆和超网络各自贡献多少 | R2-1；R3 混合网络与局部优化问题 |
| E4 | 必须做 | 奖励、探索和决策周期是否过于依赖人工选择 | R2-5；R3 单位权重与 epsilon |
| E5 | 必须做 | 冻结策略能否处理未见初始状态、扰动和参数误差 | R1-1；R2-6；R3 稳定性 |
| E6 | 第二批 | 扩大电芯数量后效果和计算代价怎样变化 | R2-4；R3 嵌入式部署讨论 |
| E7 | 第二批 | IL 能否作为低计算代价的替代或预训练方式 | R2-8 |

### E0 固定数据模型和实验协议

首先建立数据清单，记录每条序列所属电芯、循环阶段、采样间隔、放电电流、环境温度、初始状态、终止电压和单位。缺失项标为待确认，不用模型拟合结果反推成确定的实验事实。分别记录原文设定、原代码实际值和新实验选择。

区分三种数据：实测序列用于模型辨识；RL 训练轨迹由仿真交互生成；最终评估轨迹来自冻结策略。这里应写“在仿真环境中交互训练，部署评估时冻结权重”，避免把它和固定数据集上的 offline RL 混为一谈。

模型检查包括：4 A 与 5 A 假设对结果的影响、SOC 与容量定义、SPM 与 SPMe 的名称及方程一致性、热参数的合理范围，以及求解精度和网格敏感性。温度误差应按放电前段、中段、末段分别报告，并检查传感器温度与模型平均温度是否对应。增加参数前先检查可辨识性；建议 5 次 PSO 初始化，比较参数变化与误差变化，避免不同参数都能拟合却被解释为唯一老化机制。

如果目录中能找到其他完整循环或倍率曲线，按整条实验划分辨识集与验证集。如果仍只有每节一条放电曲线，可以做时间分段外推检查，但应明确这不是独立倍率或独立电芯验证。仅靠现有曲线无法完成的实测泛化验证应列为限制，不阻断后续数值研究。

公平比较首先要统一电流控制权限。建议主实验使用所有方法都可独立调节各节电流的场景，再另设共有充电电源功率上限的场景检验协作价值。共有功率约束为新实验设定，应明确标注，不伪称原系统已经具有该条件。若使用集中式电流分配或安全投影，还必须披露所需通信，不能继续声称整个控制过程完全无集中环节。

建议统一单回合物理时限为 5400 s，原 90 s 决策周期对应最多 60 次决策；这是新实验协议，与现有训练 50 步、评估 60 步的设置差别应记录。改变决策周期时保持同一物理时限，另外统计样本和控制调用数量。

**完成标准：** 实验配置无未标注矛盾；基线与待测方法使用相同被控对象、初始状态、供电预算、终止要求和允许的测量信息；数据与模型误差有可追踪记录。信息仍不完整时，结论限定在声明的仿真假设下。

### E1 安全与持续均衡

原奖励对超限进行惩罚，当前状态的动作筛选也会在超限后才响应，二者都不能单独证明下一控制周期安全。建议加入可审查的安全层：在给定模型误差裕量下限制候选电流，对下一控制区间的电压和温度进行检查；预测不可行时进入经验证的保守策略，已经无法恢复的状态应安全终止并记为失败。0 A 也不能被无条件视为温度立即安全。

先实现可解释的保守可行动作集合，再视需要升级为预测安全滤波。安全滤波的已有研究支持“策略建议动作、安全机制检查或修改动作”的分工，但正式保证依赖模型、误差界和递归可行性等假设。[预测安全滤波原始论文](https://www.sciencedirect.com/science/article/pii/S0005109821001175)

设置三组：现有 MBA-RL；MBA-RL 加安全层；SOC 规则加同一安全层。第一组只作为仿真诊断。后续算法主比较统一安全接口，避免把安全层贡献算成 QMIX 的贡献。保存原始动作、实际动作、修改原因、干预次数和干预幅度。安全层用名义模型及估计状态，被控仿真使用扰动后的参数；精确获知真实模型的版本只作理想参照。

主协议保留 4.2 V、309 K、各节 SOC ≥0.9、SOC 标准差 ≤0.02，另明确 SOC 上限。约束应检查控制间隔内部及电流切换瞬间，不能只检查每 90 s 的末点。设置 90 / 30 / 10 s 三种决策周期作为独立敏感性实验，比较时同时报告物理时长、交互步数和计算代价。

均衡至少报告首次均衡时刻、最后一次进入并保持均衡的时刻、再次失衡次数，以及从首次均衡到充电完成的超阈面积。首次达标恰好位于终点时，持续时长记为 0。可增加 270 s 的固定保持观察窗作为终端稳定性检查，但不能将停止充电后的保持替代充电过程中的持续均衡证据。

**完成标准：** 固定测试集上，记录到的全部约束违规均被如实报告；安全版本在标称可行工况中无观察到的越界，并能完成任务而非始终零电流。冻结测试集后不再据此调参。有限测试中零违规只能称为经验结果，不是绝对安全保证。

### E2 公平的算法对比

| 方法 | 必要性 | 实现要求 |
|---|---|---|
| 正确的 CC-CV | 基本工程参照 | 有真实恒压或清楚定义的采样式恒压反馈，不能把恒流加转移电流冒充 CC-CV |
| SOC 差值规则控制 | 验证是否需要 RL | 优先给落后电芯充电，接近约束时降低电流；与 RL 使用相同调流权限 |
| 新实现的集中式 DQN | 回应原 DQL 实现不清 | 三节、每节 16 档时联合动作 16³=4096；说明集中式观测优势，标为重新实现的基线 |
| IQL 独立 Q 学习 | 直接检验全局协同是否必要 | 使用相同团队奖励；分别检查共享与不共享参数，不与奖励设计变化混在一起 |
| MAPPO | 审稿人明确要求的现代 MARL 参照 | 首先采用与 QMIX 相同的离散电流集合，明确局部 actor 与训练阶段 critic 的信息 |
| MBA-RL / QMIX | 待检验方法 | 保留标准 QMIX 版本，再明确说明本研究新增的电池控制设计 |

MAPPO 原始研究在合作多智能体任务中给出较强基线结果，因此应认真调参与实现，不能预设其不如 QMIX。[MAPPO 原始论文](https://proceedings.neurips.cc/paper_files/paper/2022/hash/9c1535a02f0ce079433344e14d910597-Abstract.html)

推荐再增加 SAC，作为连续动作补充组：连续电流范围仍为 0–7.5 A，约束和电源预算相同。同时运行连续 MAPPO 或离散分辨率敏感性，以区分算法与电流量化的贡献。连续 SAC 与离散 QMIX 的差距不能全部解释为算法优劣。

纯算法对比尽量统一动作集合和信息条件；工程控制器若使用连续恒压内环，应单独说明其控制权限。集中式 DQN、SAC 或 MPC 使用全局观测时必须显式列出。训练调参用验证集，按预定相同搜索预算选配置，不照搬一组差参数来衬托主方法。

先用 3 个随机种子、每种方法 5000 个电池组决策步排除实现问题；正式比较建议至少 5 个独立训练种子，关键结论扩至 10 个。初始正式预算建议每种子 60000 个电池组决策步，统计实际完成回合数，并另保留原文 1200 回合设置的对照。相同回合数不等于相同采样量，预算不足时如实标为未收敛，不任意只给某个方法追加训练。

**完成标准：** 对每种方法同时报告任务成功率、约束违规、持续均衡与充电时间。只有在安全水平和终端状态可比时才解释速度与能耗优势。若简单规则或 IQL 已达到相同效果，应重新审视复杂网络的必要性，而不是继续只挑 A/B 曲线。

### E3 组件消融与协作机制

| 分组 | 保持不变 | 主要回答 |
|---|---|---|
| 标准 QMIX + GRU | 全部基准设定 | 完整参照 |
| GRU 换成参数量接近的 MLP | 混合网络、奖励、训练预算 | 历史观测是否有价值 |
| QMIX 换成 VDN 求和混合 | agent 网络及训练协议 | 非线性状态条件混合是否有价值；该组同时改变多项混合特性，不能单独归因于超网络 |
| 状态条件超网络换成状态无关的非负混合权重 | 保留单调性和其他网络结构 | 全局状态调节混合权重的作用 |
| 移除 mixer，使用 IQL | 保留同一团队奖励 | 全局优化是否优于各 agent 分别优化 Bellman 损失 |
| IQL 共享参数与不共享参数 | 奖励、动作、局部信息相同 | 权重共享本身的贡献 |
| 移除均衡奖励 | 网络和安全机制 | 均衡是否主要由该奖励驱动 |
| 新方法移除安全层或训练随机化 | 其余部分不变 | 新增设计分别带来什么收益 |

所有分组分别重训，不能只在已经训练好的完整网络上删除组件。主要消融建议 5 个种子，并同时报告参数量、样本效率与推理耗时。GRU 的优势应重点在观测噪声、SOC 估计误差和电芯历史状态差异下检验。

建议增加“独立电源预算”和“共有电源功率上限”两种条件。前者检验现有问题是否接近可分解，后者引入明确的资源竞争，便于判断协作何时有价值。若优势只在共有预算下出现，论文结论应限定于该条件。

理论解释可说明 QMIX 的单调结构支持集中与分散贪婪动作选择一致性，但它不等价于一般深度 MARL 全局收敛保证。训练曲线也不能被写成数学证明。[QMIX 原始论文](https://proceedings.mlr.press/v80/rashid18a.html)

### E4 奖励与训练敏感性

先比较原始权重与四个奖励项全部单位权重；这是对 R3 的直接回应。由于时间、SOC、电压和温度量纲不同，再设置“先按固定物理尺度归一化、再用单位权重”的版本，两个单位权重实验不能混为一组。归一化尺度仅由物理定义或训练数据确定。

对均衡、电压、温度三项权重分别乘 0.5、1、2，一次只改变一个因素；初筛 3 个种子，关键差异用 5 个种子确认。不需要一开始铺开大规模全因子搜索。已有数值失败扣 2000 分属于重建假设，应明确独立于原四项奖励，测试是否因过大终止惩罚主导学习。

探索比较 epsilon 从 0.5 与 1.0 起始，两者采用相同终值、衰减所需交互步数和总训练预算。epsilon=1.0 不是普适正确答案，关键是检验原设置是否导致探索不足。建议衰减后保留小幅探索至训练结束，而最终评估固定贪婪策略。

记录安全任务成功率、达标时间、过冲、停滞比例和动作分布。不同奖励定义下的累计 reward 数值不能直接作为优劣结论。

### E5 动态环境与泛化

训练集、调参验证集和最终测试集分开固定。保留 A/B 为便于对照的案例，新增至少 100 个随机初始 SOC 测试场景，以及至少 100 个按扰动类别均衡分配的测试场景。所有算法使用同一批物理场景，测试阶段冻结权重。

| 因素 | 建议起始设置 | 解释边界 |
|---|---|---|
| 初始 SOC | 低、中、高差异，并打乱电芯位置与年龄组合 | 避免只记住 A/B 或固定 agent 身份 |
| 容量及动力学误差 | 名义值附近约 ±5%、±10% 的可行参数扰动 | 是仿真压力范围，不冒充实测误差分布；检查物理一致性 |
| 环境温度 | 15、25、35 °C | 35 °C 接近 309 K 限制，先判定任务可行性 |
| 观测误差 | SOC ±1、±3 个百分点；电压 5、10 mV；温度 0.5、1 K | 区分固定偏置与随机噪声，不裁剪噪声来隐藏极端情况 |
| 环境变化 | 充电过程中环境温度增加 5 °C，或可用功率降至 70% | 动态控制适应，不等同于在线更新网络参数 |
| 延迟与丢帧 | 一次观测延迟或短时重复旧观测 | 明确策略与安全层如何获取、使用时间戳 |

上述幅值是建议的初始压力测试，不是已核实的传感器精度或电芯分布。多个因素先分别测试，再选少量组合测试，避免算力被笛卡尔积消耗。

比较“只在标称环境训练”和“在声明范围内随机化训练”，测试集包含范围内未见样本与范围外压力场景。安全层与策略不得读取被控仿真独有的真实参数和内部状态；完全可观测版本另标为理想参照。

报告均值、离散程度、95% 置信区间及失败样本。训练种子是重要的独立重复单位；同一个模型产生的几百段轨迹不能冒充几百次独立训练。可按训练种子和公共测试场景分层重采样，并报告方法之间的配对差值。建议采用更稳健的 RL 统计评价方法。[RL 统计评价研究](https://arxiv.org/abs/2108.13264)

### E6 扩展规模与计算成本

先做 3、6、12 节完整闭环仿真；预算允许再扩至 24 节。分别记录电池物理求解、训练更新、agent 推理和安全机制耗时，报告单 agent 与整组端到端时延的中位数、95 分位数、内存和网络参数量。预热后在同一机器上测量，并公开硬件与软件版本。

24、48、96 节可先做纯推理压力测试，但不能用它证明大电池组的闭环控制能力。当前实现的 agent 身份编码和混合网络输入与电芯数有关，直接复制三个 agent 不等于实现任意数量扩展。需要说明是为每个规模重新训练，还是改造为使用电芯特征的共享网络；后者属于新的架构改动，跨规模零样本泛化必须单独验证。

主实验共享资源时，明确扩容后总供电功率是否按电芯数同比增加。分别报告固定每节功率预算与固定整包功率预算，避免由供电资源变化造成误导。

本轮可给出桌面计算成本，不能据此声称已满足具体微控制器部署要求。

### E7 模仿学习与可选扩展

必须在相关工作与讨论中回应审稿人指定的两篇 IL 研究。2023 年论文以 DAgger 处理未知参数与不可观测内部状态，2025 年论文利用 IL 近似随机预测控制策略；这些工作表明 IL 是相关替代路径，不能简单声称 RL 必然更优。[2023 年研究](https://publires.unicatt.it/en/publications/imitation-learning-for-agnostic-battery-charging-a-dagger-based-a/)、[2025 年研究](https://publires.unicatt.it/it/publications/neural-network-based-imitation-learning-for-approximating-stochas/)

数值扩展可先用可信的 SOC 规则或 MPC 产生专家轨迹，比较行为克隆、DAgger 和 MBA-RL；专家信息与代价要明确。规则专家较弱时，克隆性能差不能用来否定 IL。应记录专家查询次数、数据生成成本、训练成本、最终约束和推理时间。MPC/IL 的整包改编要标为本研究实现，不能直接套用单电芯论文数字。

更有价值的后续方向是检验 IL 预训练能否减少 MBA-RL 的探索成本，但只有前面的主比较可靠后再开展。

## 四 指标与结果图的统一口径

| 类别 | 必报指标 | 避免的误解 |
|---|---|---|
| 完成任务 | 安全成功率、完成时间、未完成与失败原因 | 不删除超时或求解失败样本；未完成时间按删失处理或另报，不直接当成功时间 |
| 均衡 | 首次均衡、持续均衡起点与持续时长、SOC 标准差积分、最大单体偏差 | 大规模下标准差可能掩盖少数异常电芯 |
| 安全 | 最大电压/温度、超限峰值、超限时长或面积、干预率、终止次数 | 软惩罚小、平均温度低都不代表无超限 |
| 充入电量 | 各节积分电量及其和，单位 Ah | 各节之和不是串联电池包的额定 Ah 容量 |
| 能量 | 端口输入能量、可计算的不可逆热或电路损耗、能量指标的模型定义 | 少输入能量可能只是少充电；缺少损耗模型时不能把输入 Wh 称作损耗或效率 |
| 电池应力 | 高 SOC 停留、温度暴露、电流变化、通量及可选 I² 积分 | 这些是代理指标，不能直接换算寿命提升百分比 |
| 计算 | 训练交互量、训练时间、推理时延、全控制链时延 | 不能把 PyBaMM 仿真时间当作部署推理时间 |

充电时间、能耗比较必须检查最终 SOC 向量是否可比。当前达到目标时允许一定过冲，建议另设共同终端 SOC 容差或按相同有效储能增量比较；若做不到，逐节列出终端 SOC，并停止直接宣称节能百分比。电池内部热、电源转换损耗、均衡电路损耗应分开记账，避免重复计算。非负电流方法的放电量为零是动作约束的结果，应让同样非负的规则基线参与比较。

建议论文结果按以下顺序呈现：模型验证与数据来源；多种子训练曲线；算法性能与安全主表；A/B 的 SOC、电流、电压、温度案例图；组件消融；奖励和探索敏感性；扰动与泛化；规模和计算代价。正文展示与主张直接相关的图，其余数据放附录或公开结果目录。

没有独立循环老化验证或可靠、明确的退化模型时，结论限定为“降低某些充电应力指标”，不能写“延长寿命”。没有严格收敛证明时，报告经验收敛、失败条件和条件化的安全论证。

## 五 分批执行和停止条件

第一批完成 E0–E1，只运行现有 MBA-RL、正确 CC-CV 和 SOC 规则，先排除不可行模型、错误终止和安全机制问题。若在标称可行场景中仍普遍超限或靠不充电才安全，暂不启动大规模基线训练。

第二批完成 E2–E4，形成离散动作主比较、关键组件消融以及奖励/探索实验。先小预算检查实现，再按冻结协议正式运行。若 QMIX 对比 IQL 或规则没有稳定优势，应检查任务耦合与信息条件，并按结果调整贡献定位。

第三批完成 E5–E6，测试冻结策略的泛化及规模成本。连续动作 SAC、IL 和退化模型可按研究重点追加，避免为了增加算法数量而降低每项实验质量。

正式算法主比较可从 6 种方法 × 5 个种子 × 60000 个电池组决策步开始，即约 180 万组决策步；不含调参、消融和评估。预算是起点，不保证各方法都收敛。先测各算法实际吞吐量再估算工期；当前 3 节 QMIX 速度不能直接外推到 MAPPO、SAC 或大规模仿真。

后续代码建议在现有 `mainline_reproduction` 基础上增加统一环境接口、基线、实验配置和统计脚本；保留旧版结果作为诊断记录。每个结果文件夹应包含配置、代码版本、数据哈希、随机种子、模型、逐回合轨迹、统计表和失败原因。训练策略、验证选模与最终测试严格分离。

## 六 论文贡献与审稿回复

新的贡献应由实验决定。若结果支持，可以围绕异质电芯协作、可审查的约束处理和模型误差下的稳健性组织全文；标准 QMIX、GRU 和通用安全滤波本身已有文献，不能作为新算法重新命名。需要清楚说明电池问题中的新增机制以及它在消融中带来的收益。

R1 的动态适应与数据问题由 E0/E5 回答；R1/R2 的先进基线由 E2 回答；R2/R3 的网络必要性由 E3 回答；奖励与探索由 E4 回答；计算开销由 E6 回答；能量与健康指标按第四节补充；IL 由 E7 回答。每条回复应列出具体改动位置、实验条件、数值结果和限制。

R3 关于公式、离散动作、训练与部署的疑问需要直接改正文与算法伪代码。其希望使用非人称表达属于写作修改，不能代替技术问题的实质回应。硬件在线控制与微控制器建议在本轮明确列为尚未完成。

拒稿信日期为 2025 年 6 月 30 日，信中给出的 12 个月重投窗口按日期计算已于 2026 年 6 月 30 日结束。当前为 2026 年 9 月 19 日，能否沿用原稿重投通道需另向编辑部确认；本规划不假定该通道仍有效，也没有执行任何投稿或联系操作。

## 七 阅读依据

- 用户文件：`C:/Users/zhengzihao/Desktop/RL-for-charging-main/拒稿信.txt`。
- 用户文件：`C:/Users/zhengzihao/Desktop/RL-for-charging-main/锂离子电池充电控制的多平衡智能体强化学习(1).docx`，已读取正文及包含式（6）质疑的内嵌图片。
- 原论文：`C:/Users/zhengzihao/Desktop/RL-for-charging-main/MARL charging balance(1).pdf` 的已提取正文，用于复核式（6）、电池型号和实验连接条件。
- 已完成实验：`C:/Users/zhengzihao/Desktop/RL-for-charging-main/mainline_results/20260919_222223_369351`，包括 `status.json`、`verification.json` 和结果报告。
- 方法文献链接已放在各项建议旁，用于核对技术依据。实验规模、阈值组合和执行顺序为本次提出的规划，不代表引用论文的原始实验设置。
